baseurl = "https://www.nomoreforloops.com/"
relativeurls = true
languageCode = "en-us"
title = "No More For Loops"
theme = "Mainroad"
googleAnalytics = "UA-90533434-1"
disqusShortname = "priority-details"
ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "_cache$", "(^|/)renv$", "renv\\.lock$"]


[permalinks]
    post = "/:year/:month/:slug/"
    page = "/:slug/"


[[menu.main]]
    name = "About"
    url = "/about/"
    weight = -70
    

[Params]
  subtitle = "I don't write for loops" # Subtitle of your site. Used in site header
  description = "Data, statistics, R, and everything data science." # Site description. Used in meta description
  copyright = "Yuchen Wang" # copyright holder, otherwise will use site title
  opengraph = true # Enable OpenGraph if true
  twitter_cards = true # Enable Twitter Cards if true
  readmore = true # Show "Read more" button in list if true
  authorbox = false # Show authorbox at bottom of pages if true
  toc = false # Enable Table of Contents
  post_navigation = true # Show post navigation at bottom of pages if true
  post_meta = ["date", "categories", "translations"] # Order of post meta information. Use ["none"] to turn off completely.
  postSections = ["post"] # the section pages to show on home page and the "Recent articles" widget
  #postSections = ["blog", "news"] # alternative that shows more than one section's pages
  dateformat = "2006-01-02" # change the format of dates
  mathjax = true # Enable MathJax
  mathjaxPath = "https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/MathJax.js" # Specify MathJax path
  mathjaxConfig = "TeX-AMS-MML_HTMLorMML" # Specify MathJax config
  # customCSS = ["css/custom.css"] # Include custom CSS files

[Params.sidebar]
  home = "right" # Configure layout for home page
  list = "right"  # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["social", "search", "recent", "categories", "taglist", "languages"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)

[Params.widgets.social]
  linkedin = "yuchen0712"
  github = "wangyuchen"


[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

